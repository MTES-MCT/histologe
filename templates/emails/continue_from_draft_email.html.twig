{% extends 'emails/base_email.html.twig' %}

{% block body %}
    <p>
        Le {{ signalement_draft.createdAt.format('d/m/Y') }}, vous avez commencé un signalement sur {{ platform.name }} pour le logement situé {{ signalement_draft.addressComplete }}.
        Pour compléter votre signalement et le valider, cliquez sur le bouton ci-dessous.
    </p>
    
    <table role="presentation" border="0" cellpadding="0" cellspacing="0" class="btn btn-primary"
           style="text-align: center">
        <tbody>
        <tr>
            <td align="left">
                <table role="presentation" border="0" cellpadding="0" cellspacing="0">
                    <tbody>
                    <tr style="text-align: center;width: 100%">
                        <td>
                            <a href="{{ lien_draft|raw }}" target="_blank" rel="noopener">{{ btntext }}</a>
                        </td>
                    </tr>
                    </tbody>
                </table>
            </td>
        </tr>
        </tbody>
    </table>
    <p>Si le bouton ne fonctionne pas <a href="{{ lien_draft|raw }}">cliquez ici</a></p>
{% endblock %}
