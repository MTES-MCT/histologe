{% extends 'emails/base_email.html.twig' %}

{% block body %}
    <p>Bonjour,</p>
    <p>
        Les comptes ci-dessous ne se sont pas connectés à la plateforme {{ platform.name }} depuis presque un an.
        <br>
        Sans action de leur part, les comptes suivants seront supprimés dans 15 jours :
    </p>
    <ul>
        {% for user in usersData %}
            <li>{{ user.nom|upper }} {{user.prenom|capitalize}} - {{user.email}} {{user.partenaire}}</li>
        {% endfor %}
    </ul>
    <p>
        Vous pouvez prévenir ces utilisateurs par mail et leur demander de se connecter. Cliquez sur le bouton pour visualiser la liste des comptes utilisateurs concernés.
    </p>

    <table role="presentation" border="0" cellpadding="0" cellspacing="0" class="btn btn-primary" style="text-align: center">
        <tbody>
            <tr>
                <td align="left">
                    <table role="presentation" border="0" cellpadding="0" cellspacing="0">
                        <tbody>
                            <tr style="text-align: center;width: 100%">
                                <td><a href="{{ link|raw }}" target="_blank" rel="noopener">{{ btntext }}</a></td>
                            </tr>
                        </tbody>
                    </table>
                </td>
            </tr>
        </tbody>
    </table>
    <p>Si le bouton ne fonctionne pas <a href="{{ link|raw }}">cliquez ici</a></p>
{% endblock %}
