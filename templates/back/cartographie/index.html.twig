{% extends 'back/base_bo.html.twig' %}

{% block title %}Cartographie{% endblock %}

{% block content %}
    <section class="fr-pt-5v {{ app.request.attributes.get('_route') == 'back_signalement_index' ? container_class : 'fr-pl-5v' }}">
        {% include 'back/breadcrumb_bo.html.twig' with {
            'level2Title': 'Données chiffrées',
            'level2Link': '',
            'level2Label': '',
            'level3Title': 'Cartographie',
            'level3Link': '',
        } %}
    </section>
    <div id="app-signalement-carto" class="cartography-page"
         data-ajaxurl="{{ path('back_signalement_carto_json') }}"
         data-ajaxurl-settings="{{ path('back_settings') }}"
         data-token="{{ csrf_token('load_markers_'~''|date('dmYhi')) }}"
    >

    </div>
{% endblock %}
