<dialog aria-labelledby="panel-deny-affectation-title" id="panel-deny-affectation" data-panel-visite="true" class="side-panel">
    <div class="side-panel__inner">
        <div class="side-panel__header">
            <button type="button" class="fr-btn--close fr-btn js-close-panel" data-panel="panel-deny-affectation" aria-controls="panel-deny-affectation" aria-expanded="false">Fermer</button>
        </div>

        <div class="side-panel__content fr-mx-3w">
            <h1 id="panel-deny-affectation-title" class="fr-modal__title title-blue-france"
                >Refuser le signalement #{{ signalement.reference }}</h1>

            {{ form_start(refusAffectationForm, {'attr': {'id': 'form-deny-affectation', 'data-submit-type':'formData'}}) }}
            {{ form_errors(refusAffectationForm) }}
            {{ form_row(refusAffectationForm.motifRefus) }}
            {{ form_row(refusAffectationForm.description) }}
            {{ form_row(refusAffectationForm.files) }}
            {{ form_end(refusAffectationForm) }}
        </div>
        <div class="side-panel__footer">
            <ul class="fr-btns-group fr-btns-group--right fr-btns-group--inline-reverse fr-btns-group--inline-sm fr-btns-group--icon-left">
                <li>
                    <button type="submit"
                        class="fr-btn fr-btn--icon-left fr-icon-check-line"
                        form="form-deny-affectation"
                        >Valider</button>
                </li>
                <li>
                    <button type="button"
                        class="fr-btn fr-btn--secondary fr-icon-close-line js-close-panel"
                        data-panel="panel-deny-affectation"
                        aria-controls="panel-deny-affectation"
                        aria-expanded="false"
                        >Annuler</button>
                </li>
            </ul>
        </div>
    </div>
</dialog>