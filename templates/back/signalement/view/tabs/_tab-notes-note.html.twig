{% if userNote and userNote.content %}
    <div class="signalement-note-display">
        <div class="fr-p-3w fr-background-alt--grey fr-mb-3v">{{ userNote.content|raw }}</div>
        <ul class="fr-btns-group fr-btns-group--inline fr-btns-group--icon-left">
            <li>
                <button type="button" class="fr-btn fr-btn--secondary fr-btn--icon-left fr-icon-edit-line signalement-note-edit-btn">
                    Modifier
                </button>
            </li>
            <li>
                <div class="single-ajax-form-container">
                    <form method="POST" action="{{ path('back_signalement_delete_note', {uuid: signalement.uuid}) }}" data-submit-type="formData">
                        <input type="hidden" name="_token" value="{{ csrf_token('signalement_delete_note') }}">
                        <button type="submit" class="fr-btn fr-btn--secondary fr-btn--icon-left fr-icon-delete-line">
                            Supprimer
                        </button>
                    </form>
                </div>
            </li>
        </ul>
    </div>
    <div class="signalement-note-edit fr-hidden">
        <div class="single-ajax-form-container">
            <form method="POST" action="{{ path('back_signalement_save_note', {uuid: signalement.uuid}) }}" data-submit-type="formData">
                <div class="fr-input-group">
                    <label class="fr-label" for="note-content">Votre note personnelle</label>
                    <textarea class="fr-input editor" id="note-content" name="content" rows="8">{{ userNote.content|raw }}</textarea>
                </div>
                <input type="hidden" name="_token" value="{{ csrf_token('signalement_save_note') }}">
                <ul class="fr-btns-group fr-btns-group--right fr-btns-group--inline fr-btns-group--icon-left">
                    <li>
                        <button class="fr-btn fr-btn--icon-left fr-icon-check-line" type="submit">Enregistrer</button>
                    </li>
                    <li>
                        <button type="button" class="fr-btn fr-btn--secondary fr-btn--icon-left fr-icon-close-line signalement-note-cancel-btn">Annuler</button>
                    </li>
                </ul>
            </form>
        </div>
    </div>
{% else %}
    <div class="signalement-note-edit">
        <div class="single-ajax-form-container">
            <form method="POST" action="{{ path('back_signalement_save_note', {uuid: signalement.uuid}) }}" data-submit-type="formData">
                <div class="fr-input-group">
                    <label class="fr-label" for="note-content">Votre note personnelle</label>
                    <textarea class="fr-input editor" id="note-content" name="content" rows="8" placeholder="Saisissez votre note personnelle..."></textarea>
                </div>
                <input type="hidden" name="_token" value="{{ csrf_token('signalement_save_note') }}">
                <ul class="fr-btns-group fr-btns-group--right fr-btns-group--inline fr-btns-group--icon-left">
                    <li>
                        <button class="fr-btn fr-btn--icon-left fr-icon-check-line" type="submit">Enregistrer</button>
                    </li>
                </ul>
            </form>
        </div>
    </div>
{% endif %}
