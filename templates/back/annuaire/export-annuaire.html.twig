{% extends 'back/base_bo.html.twig' %}

{% block title %}Export de l'annuaire des agents{% endblock %}

{% block content %}
    <section class="fr-p-5v">
        {% include 'back/breadcrumb_bo.html.twig' with {
            'level2Title': 'Annuaire',
            'level2Link': path('back_annuaire_index', searchAnnuaire.urlParams),
            'level2Label': '',
            'level3Title': 'Export',
            'level3Link': '',
            'level3Label': '',
        } %}

        <header>
            <div class="fr-grid-row">
                <div class="fr-col-12 fr-text--left">
                    <h1>Exporter l'annuaire ({{nbResults}} agent(s))</h1>
                    <p>
                        Vous êtes sur le point d'exporter l'annuaire.
                        <br>
                        Choisissez le format de fichier que vous souhaitez recevoir.
                    </p>
                </div>
            </div>
        </header>
    </section>

    <section class="fr-p-5v">
        <h2>Filtres actifs</h2>
        <div class="fr-mb-3v">
            Votre liste contient {{ nbResults }} agents. Les filtres suivants sont actifs :
        </div>
        {% for filterName, filterValue in searchAnnuaire.filtersToText %}
            <p class="fr-badge">{{ filterName }} : {{ filterValue }}</p>
        {% else %}
            Aucun filtre actif.
        {% endfor %}
    </section>

    <section class="fr-p-5v">
        <form action="" method="POST" id="export-annuaire">
            <h2>Format</h2>
            {% include 'form/form-choice-export-format.html.twig' %}
        </form>

        <h2 class="fr-mt-5v">Informations du fichier</h2>
        <div class="fr-alert fr-alert--warning fr-alert--sm">
            <p>Les informations contenues dans ce fichier ne doivent en aucun cas être partagées en dehors de vos services.</p>
        </div>
        <p class="fr-mt-5v">
            Votre fichier contient les informations suivantes :
        </p>
        <ul>
            <li><strong>Nom complet de l'agent</strong> : le nom et le prénom de l'agent</li>
            <li><strong>Nom du partenaire</strong> : le nom du partenaire auquel l'agent appartient</li>
            <li><strong>Adresse e-mail de l'agent</strong> : l'adresse e-mail du compte</li>
            <li><strong>Téléphone de l'agent</strong> : le numéro de téléphone du compte</li>
            <li><strong>Fonction de l'agent</strong> : la fonction de l'agent dans son partenaire</li>
            {% if isMultiTerritory %}                
                <li><strong>Territoire</strong> : le territoire auquel appartient le partenaire</li>
            {% endif %}
        </ul>
        <ul class="fr-btns-group fr-btns-group--inline-lg fr-btns-group--right fr-btns-group--icon-left">
            <li>
                <a href="{{ path('back_annuaire_index') }}" class="fr-btn fr-w-100 fr-btn--secondary fr-btn--icon-left fr-icon-close-line">
                    Annuler
                </a>
            </li>
            <li>
                <button type="submit" form="export-annuaire" class="fr-btn fr-w-100 fr-btn--icon-left fr-icon-check-line">
                    Exporter le fichier
                </button>
            </li>
        </ul>
    </section>

{% endblock %}
