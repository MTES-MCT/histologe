{% extends 'back/base_bo.html.twig' %}

{% block title %}Modifier la zone {{zone.name}}{% endblock %}

{% block content %}
    <section class="fr-p-5v">
        {% include 'back/breadcrumb_bo.html.twig' with {
            'level2Title': 'Outils admin',
            'level2Link': '',
            'level2Label': '',
            'level3Title': 'Zones',
            'level3Link': path('back_zone_index'),
            'level4Title': 'Zone '~ zone.name,
            'level4Link': path('back_zone_show', {zone: zone.id}),
            'level4Label': 'Retour à la zone',
            'level5Title': 'Modifier la zone ' ~ zone.name,
            'level5Link': '',
            'level5Label': '',
        } %}
        <header>
            <div class="fr-grid-row">
                <div class="fr-col-12 fr-text--left">
                    <h1>Zone {{zone.name}}</h1>
                </div>
            </div>
        </header>
    </section>

    <section class="fr-grid-row fr-px-5v">
        <div class="fr-col-12 fr-col-lg-6">
            {% form_theme form 'form/dsfr_theme.html.twig' %}
            {{form(form, {'attr': {'id': 'form-add-zone'}} )}}
        </div>
    </section>

{% endblock %}