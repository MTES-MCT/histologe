grumphp:
    tasks:
      git_blacklist:
        keywords:
          - 'var_dump('
          - 'dump('
          - ' dd('
          - ';dd('
          - 'die('
          - 'exit('
      phpstan: ~
      shell:
        scripts:
          - ["-c", "make cs-check"]
