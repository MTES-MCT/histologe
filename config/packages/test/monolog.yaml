monolog:
    handlers:
        main:
            type: test
            level: debug
        file:
            type: stream
            path: '%kernel.logs_dir%/%kernel.environment%.log'
            level: debug
